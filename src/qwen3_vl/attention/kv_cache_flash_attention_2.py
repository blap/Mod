"""\nKV Cache Optimized FlashAttention 2 implementation for Qwen3-VL model.\nCombines FlashAttention 2 memory efficiency with KV cache optimization strategies.\n"""\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport math\nfrom typing import Optional, Tuple, Union, List\nfrom src.qwen3_vl.core.config import Qwen3VLConfig\nfrom attention.flash_attention_2 import FlashAttention2, SM61OptimizedFlashAttention2, apply_rotary_pos_emb, repeat_kv, Qwen3VLRotaryEmbedding\n\n\nclass KVCacheOptimizedFlashAttention2(nn.Module):\n    """\n    FlashAttention 2 with integrated KV cache optimization strategies:\n    - Low-rank approximation for KV cache compression\n    - Sliding window attention to limit cache size\n    - Memory-efficient attention computation patterns\n    """\n    def __init__(self, config: Qwen3VLConfig, layer_idx: Optional[int] = None):\n        super().__init__()\n        self.config = config\n        self.layer_idx = layer_idx\n\n        self.hidden_size = config.hidden_size\n        self.num_heads = config.num_attention_heads\n        self.head_dim = self.hidden_size // self.num_heads\n        self.num_key_value_heads = config.num_key_value_heads or self.num_heads\n        self.num_key_value_groups = self.num_heads // self.num_key_value_heads\n        self.max_position_embeddings = config.max_position_embeddings\n        self.rope_theta = config.rope_theta\n        self.kv_cache_window_size = getattr(config, 'kv_cache_window_size', 1024)  # Window size for sliding window\n        self.low_rank_factor = getattr(config, 'kv_cache_low_rank_factor', 64)  # Rank for low-rank approximation\n\n        if (self.head_dim * self.num_heads) != self.hidden_size:\n            raise ValueError(\n                f"hidden_size must be divisible by num_heads (got `hidden_size`: {self.hidden_size}"\n                f" and `num_heads`: {self.num_heads})."\n            )\n\n        # Initialize projections\n        self.q_proj = nn.Linear(self.hidden_size, self.num_heads * self.head_dim, bias=True)\n        self.k_proj = nn.Linear(self.hidden_size, self.num_key_value_heads * self.head_dim, bias=True)\n        self.v_proj = nn.Linear(self.hidden_size, self.num_key_value_heads * self.head_dim, bias=True)\n        self.o_proj = nn.Linear(self.num_heads * self.head_dim, self.hidden_size, bias=True)\n\n        # Rotary embedding for position encoding\n        self.rotary_emb = Qwen3VLRotaryEmbedding(\n            self.head_dim,\n            max_position_embeddings=self.max_position_embeddings,\n            base=self.rope_theta,\n        )\n\n        # Low-rank approximation components for KV cache compression\n        self.k_low_rank_proj = nn.Linear(self.head_dim, self.low_rank_factor, bias=False)\n        self.k_low_rank_out = nn.Linear(self.low_rank_factor, self.head_dim, bias=False)\n        self.v_low_rank_proj = nn.Linear(self.head_dim, self.low_rank_factor, bias=False)\n        self.v_low_rank_out = nn.Linear(self.low_rank_factor, self.head_dim, bias=False)\n\n        # Memory-efficient parameters\n        self.tile_size = 512  # Size of tiles for chunked computation\n        self.use_memory_efficient_kernel = True  # Whether to use the memory-efficient kernel\n\n    def forward(\n        self,\n        hidden_states: torch.Tensor,\n        attention_mask: Optional[torch.Tensor] = None,\n        position_ids: Optional[torch.LongTensor] = None,\n        past_key_value: Optional[Tuple[torch.Tensor]] = None,\n        output_attentions: bool = False,\n        use_cache: bool = False,\n        cache_position: Optional[torch.LongTensor] = None,\n    ) -> Tuple[torch.Tensor, Optional[torch.Tensor], Optional[Tuple[torch.Tensor]]]:\n        bsz, q_len, _ = hidden_states.size()\n\n        # Project queries, keys, and values\n        query_states = self.q_proj(hidden_states)\n        key_states = self.k_proj(hidden_states)\n        value_states = self.v_proj(hidden_states)\n\n        # Reshape to multi-head format\n        query_states = query_states.view(bsz, q_len, self.num_heads, self.head_dim).transpose(1, 2)\n        key_states = key_states.view(bsz, q_len, self.num_key_value_heads, self.head_dim).transpose(1, 2)\n        value_states = value_states.view(bsz, q_len, self.num_key_value_heads, self.head_dim).transpose(1, 2)\n\n        # Apply rotary position embeddings\n        if position_ids is None:\n            position_ids = torch.arange(q_len, dtype=torch.long, device=hidden_states.device).unsqueeze(0).expand(bsz, -1)\n\n        cos, sin = self.rotary_emb(value_states, position_ids)\n        query_states, key_states = apply_rotary_pos_emb(query_states, key_states, cos, sin)\n\n        # Apply low-rank approximation to keys and values for KV cache compression\n        key_states_low_rank = self.k_low_rank_proj(key_states)\n        value_states_low_rank = self.v_low_rank_proj(value_states)\n\n        # Handle past key values for caching with sliding window and low-rank approximation\n        if past_key_value is not None:\n            # Get existing low-rank KV cache\n            past_key_low_rank, past_value_low_rank = past_key_value\n            \n            # Apply sliding window to keep only recent tokens within window\n            if cache_position is not None:\n                # Use only the tokens within the sliding window\n                window_start = max(0, cache_position.max().item() - self.kv_cache_window_size)\n                past_key_low_rank = past_key_low_rank[:, :, window_start:cache_position.max().item() + 1, :]\n                past_value_low_rank = past_value_low_rank[:, :, window_start:cache_position.max().item() + 1, :]\n            else:\n                # If no cache_position, limit to window size\n                if past_key_low_rank.size(2) > self.kv_cache_window_size:\n                    past_key_low_rank = past_key_low_rank[:, :, -self.kv_cache_window_size:, :]\n                    past_value_low_rank = past_value_low_rank[:, :, -self.kv_cache_window_size:, :]\n            \n            # Concatenate with current low-rank KV states\n            key_states_low_rank = torch.cat([past_key_low_rank, key_states_low_rank], dim=2)\n            value_states_low_rank = torch.cat([past_value_low_rank, value_states_low_rank], dim=2)\n        else:\n            # If no past key value, just use current states\n            pass\n\n        # Reconstruct keys and values from low-rank representations\n        key_states = self.k_low_rank_out(key_states_low_rank)\n        value_states = self.v_low_rank_out(value_states_low_rank)\n\n        # Repeat keys and values for GQA (Grouped Query Attention) if applicable\n        key_states = repeat_kv(key_states, self.num_key_value_groups)\n        value_states = repeat_kv(value_states, self.num_key_value_groups)\n\n        # Memory-efficient attention computation using FlashAttention approach\n        if self.use_memory_efficient_kernel and not output_attentions:\n            # Use PyTorch's optimized scaled dot-product attention (FlashAttention-like)\n            attn_output = F.scaled_dot_product_attention(\n                query_states, key_states, value_states,\n                attn_mask=attention_mask,\n                dropout_p=0.0,  # No dropout during inference\n                is_causal=False if attention_mask is not None else True  # Set based on mask\n            )\n            attn_weights = None  # Not computed when output_attentions is False\n        else:\n            # Memory-efficient tiled computation for when output_attentions is needed\n            attn_weights = self._memory_efficient_attention_weights(\n                query_states, key_states, attention_mask\n            )\n            attn_weights = nn.functional.softmax(attn_weights, dim=-1, dtype=torch.float32).to(query_states.dtype)\n            attn_output = torch.matmul(attn_weights, value_states)\n\n        attn_output = attn_output.transpose(1, 2).contiguous()\n        attn_output = attn_output.reshape(bsz, q_len, self.hidden_size)\n\n        attn_output = self.o_proj(attn_output)\n\n        # Prepare KV cache for next iteration (using low-rank representations)\n        if use_cache:\n            # Return low-rank representations for more efficient caching\n            past_key_value = (key_states_low_rank, value_states_low_rank)\n        else:\n            past_key_value = None\n\n        return attn_output, attn_weights, past_key_value\n\n    def _memory_efficient_attention_weights(self, query_states, key_states, attention_mask):\n        """\n        Compute attention weights in a memory-efficient way using tiling.\n        This reduces memory complexity from O(n²) to O(n) by processing in chunks.\n        """\n        bsz, num_heads, seq_len, head_dim = query_states.shape\n        _, _, kv_seq_len, _ = key_states.shape\n\n        # Calculate tile size based on available memory\n        tile_size = min(self.tile_size, seq_len)\n        \n        # Initialize output tensor\n        attn_weights = torch.zeros(bsz, num_heads, seq_len, kv_seq_len, \n                                   dtype=query_states.dtype, device=query_states.device)\n\n        # Process in tiles to limit memory usage\n        for q_start in range(0, seq_len, tile_size):\n            q_end = min(q_start + tile_size, seq_len)\n            q_tile = query_states[:, :, q_start:q_end, :]\n\n            for k_start in range(0, kv_seq_len, tile_size):\n                k_end = min(k_start + tile_size, kv_seq_len)\n                k_tile = key_states[:, :, k_start:k_end, :]\n\n                # Compute attention scores for this tile\n                scores_tile = torch.matmul(q_tile, k_tile.transpose(-2, -1)) / math.sqrt(self.head_dim)\n\n                # Apply attention mask if provided\n                if attention_mask is not None:\n                    mask_tile = attention_mask[:, :, q_start:q_end, k_start:k_end]\n                    scores_tile = scores_tile + mask_tile\n\n                # Store the tile in the full attention matrix\n                attn_weights[:, :, q_start:q_end, k_start:k_end] = scores_tile\n\n        return attn_weights\n\n\nclass SM61OptimizedKVCacheFlashAttention2(nn.Module):\n    """\n    NVIDIA SM61 optimized FlashAttention 2 with KV cache optimizations.\n    """\n    def __init__(self, config: Qwen3VLConfig, layer_idx: Optional[int] = None):\n        super().__init__()\n        self.config = config\n        self.layer_idx = layer_idx\n\n        self.hidden_size = config.hidden_size\n        self.num_heads = config.num_attention_heads\n        self.head_dim = self.hidden_size // self.num_heads\n        self.num_key_value_heads = config.num_key_value_heads or self.num_heads\n        self.num_key_value_groups = self.num_heads // self.num_key_value_heads\n        self.max_position_embeddings = config.max_position_embeddings\n        self.rope_theta = config.rope_theta\n        self.kv_cache_window_size = getattr(config, 'kv_cache_window_size', 1024)  # Window size for sliding window\n        self.low_rank_factor = getattr(config, 'kv_cache_low_rank_factor', 32)  # Smaller rank for SM61\n\n        if (self.head_dim * self.num_heads) != self.hidden_size:\n            raise ValueError(\n                f"hidden_size must be divisible by num_heads (got `hidden_size`: {self.hidden_size}"\n                f" and `num_heads`: {self.num_heads})."\n            )\n\n        # Initialize projections\n        self.q_proj = nn.Linear(self.hidden_size, self.num_heads * self.head_dim, bias=True)\n        self.k_proj = nn.Linear(self.hidden_size, self.num_key_value_heads * self.head_dim, bias=True)\n        self.v_proj = nn.Linear(self.hidden_size, self.num_key_value_heads * self.head_dim, bias=True)\n        self.o_proj = nn.Linear(self.num_heads * self.head_dim, self.hidden_size, bias=True)\n\n        # Rotary embedding for position encoding\n        self.rotary_emb = Qwen3VLRotaryEmbedding(\n            self.head_dim,\n            max_position_embeddings=self.max_position_embeddings,\n            base=self.rope_theta,\n        )\n\n        # Low-rank approximation components for KV cache compression (optimized for SM61)\n        self.k_low_rank_proj = nn.Linear(self.head_dim, self.low_rank_factor, bias=False)\n        self.k_low_rank_out = nn.Linear(self.low_rank_factor, self.head_dim, bias=False)\n        self.v_low_rank_proj = nn.Linear(self.head_dim, self.low_rank_factor, bias=False)\n        self.v_low_rank_out = nn.Linear(self.low_rank_factor, self.head_dim, bias=False)\n\n        # SM61-specific parameters for optimal performance\n        self.tile_size = 256  # Smaller tile size for SM61's memory constraints\n        self.use_async_ops = True  # Use async memory operations if available\n\n    def forward(\n        self,\n        hidden_states: torch.Tensor,\n        attention_mask: Optional[torch.Tensor] = None,\n        position_ids: Optional[torch.LongTensor] = None,\n        past_key_value: Optional[Tuple[torch.Tensor]] = None,\n        output_attentions: bool = False,\n        use_cache: bool = False,\n        cache_position: Optional[torch.LongTensor] = None,\n    ) -> Tuple[torch.Tensor, Optional[torch.Tensor], Optional[Tuple[torch.Tensor]]]:\n        bsz, q_len, _ = hidden_states.size()\n\n        # Project queries, keys, and values\n        query_states = self.q_proj(hidden_states)\n        key_states = self.k_proj(hidden_states)\n        value_states = self.v_proj(hidden_states)\n\n        # Reshape to multi-head format\n        query_states = query_states.view(bsz, q_len, self.num_heads, self.head_dim).transpose(1, 2)\n        key_states = key_states.view(bsz, q_len, self.num_key_value_heads, self.head_dim).transpose(1, 2)\n        value_states = value_states.view(bsz, q_len, self.num_key_value_heads, self.head_dim).transpose(1, 2)\n\n        # Apply rotary position embeddings\n        if position_ids is None:\n            position_ids = torch.arange(q_len, dtype=torch.long, device=hidden_states.device).unsqueeze(0).expand(bsz, -1)\n\n        cos, sin = self.rotary_emb(value_states, position_ids)\n        query_states, key_states = apply_rotary_pos_emb(query_states, key_states, cos, sin)\n\n        # Apply low-rank approximation to keys and values for KV cache compression\n        key_states_low_rank = self.k_low_rank_proj(key_states)\n        value_states_low_rank = self.v_low_rank_proj(value_states)\n\n        # Handle past key values for caching with sliding window and low-rank approximation\n        if past_key_value is not None:\n            # Get existing low-rank KV cache\n            past_key_low_rank, past_value_low_rank = past_key_value\n            \n            # Apply sliding window to keep only recent tokens within window\n            if cache_position is not None:\n                # Use only the tokens within the sliding window\n                window_start = max(0, cache_position.max().item() - self.kv_cache_window_size)\n                past_key_low_rank = past_key_low_rank[:, :, window_start:cache_position.max().item() + 1, :]\n                past_value_low_rank = past_value_low_rank[:, :, window_start:cache_position.max().item() + 1, :]\n            else:\n                # If no cache_position, limit to window size\n                if past_key_low_rank.size(2) > self.kv_cache_window_size:\n                    past_key_low_rank = past_key_low_rank[:, :, -self.kv_cache_window_size:, :]\n                    past_value_low_rank = past_value_low_rank[:, :, -self.kv_cache_window_size:, :]\n            \n            # Concatenate with current low-rank KV states\n            key_states_low_rank = torch.cat([past_key_low_rank, key_states_low_rank], dim=2)\n            value_states_low_rank = torch.cat([past_value_low_rank, value_states_low_rank], dim=2)\n        else:\n            # If no past key value, just use current states\n            pass\n\n        # Reconstruct keys and values from low-rank representations\n        key_states = self.k_low_rank_out(key_states_low_rank)\n        value_states = self.v_low_rank_out(value_states_low_rank)\n\n        # Repeat keys and values for GQA (Grouped Query Attention) if applicable\n        key_states = repeat_kv(key_states, self.num_key_value_groups)\n        value_states = repeat_kv(value_states, self.num_key_value_groups)\n\n        # SM61-optimized attention computation using memory-efficient approach\n        if not output_attentions:\n            # Use PyTorch's optimized scaled dot-product attention (FlashAttention-like)\n            attn_output = F.scaled_dot_product_attention(\n                query_states, key_states, value_states,\n                attn_mask=attention_mask,\n                dropout_p=0.0,  # No dropout during inference\n                is_causal=False if attention_mask is not None else True  # Set based on mask\n            )\n            attn_weights = None  # Not computed when output_attentions is False\n        else:\n            # For SM61, compute attention weights in a memory-efficient way using tiling\n            attn_weights = self._sm61_memory_efficient_attention_weights(\n                query_states, key_states, attention_mask\n            )\n            attn_weights = nn.functional.softmax(attn_weights, dim=-1, dtype=torch.float32).to(query_states.dtype)\n            attn_output = torch.matmul(attn_weights, value_states)\n\n        attn_output = attn_output.transpose(1, 2).contiguous()\n        attn_output = attn_output.reshape(bsz, q_len, self.hidden_size)\n\n        attn_output = self.o_proj(attn_output)\n\n        # Prepare KV cache for next iteration (using low-rank representations)\n        if use_cache:\n            # Return low-rank representations for more efficient caching\n            past_key_value = (key_states_low_rank, value_states_low_rank)\n        else:\n            past_key_value = None\n\n        return attn_output, attn_weights, past_key_value\n\n    def _sm61_memory_efficient_attention_weights(self, query_states, key_states, attention_mask):\n        """\n        SM61-optimized memory-efficient attention computation with smaller tile sizes\n        and memory access patterns optimized for compute capability 6.1.\n        """\n        bsz, num_heads, seq_len, head_dim = query_states.shape\n        _, _, kv_seq_len, _ = key_states.shape\n\n        # SM61-optimized tile size (smaller for better cache utilization)\n        tile_size = min(self.tile_size, seq_len)\n        \n        # Initialize output tensor\n        attn_weights = torch.zeros(bsz, num_heads, seq_len, kv_seq_len, \n                                   dtype=query_states.dtype, device=query_states.device)\n\n        # Process in tiles with SM61-optimized access patterns\n        for q_start in range(0, seq_len, tile_size):\n            q_end = min(q_start + tile_size, seq_len)\n            q_tile = query_states[:, :, q_start:q_end, :]\n\n            for k_start in range(0, kv_seq_len, tile_size):\n                k_end = min(k_start + tile_size, kv_seq_len)\n                k_tile = key_states[:, :, k_start:k_end, :]\n\n                # Compute attention scores for this tile\n                scores_tile = torch.matmul(q_tile, k_tile.transpose(-2, -1)) / math.sqrt(self.head_dim)\n\n                # Apply attention mask if provided\n                if attention_mask is not None:\n                    mask_tile = attention_mask[:, :, q_start:q_end, k_start:k_end]\n                    scores_tile = scores_tile + mask_tile\n\n                # Store the tile in the full attention matrix\n                attn_weights[:, :, q_start:q_end, k_start:k_end] = scores_tile\n\n        return attn_weights\n\n\ndef create_optimized_flash_attention_with_cache(config: Qwen3VLConfig, layer_idx: int):\n    """\n    Factory function to create the appropriate FlashAttention 2 implementation\n    based on hardware configuration and optimization requirements.\n    """\n    if config.hardware_specific_attention and config.hardware_specific_attention == "sm61":\n        return SM61OptimizedKVCacheFlashAttention2(config, layer_idx=layer_idx)\n    else:\n        return KVCacheOptimizedFlashAttention2(config, layer_idx=layer_idx)