"""\nModel factory for Qwen3-VL.\n\nThis module provides factory functions for creating different variants of the Qwen3-VL model.\n"""\nfrom typing import Optional\nfrom models.base_model import Qwen3VLModel, create_model_from_config, create_model_from_pretrained\nfrom qwen3_vl.config.base_config import Qwen3VLConfig\n\n\ndef get_model(\n    config: Optional[Qwen3VLConfig] = None,\n    pretrained_model_name_or_path: Optional[str] = None,\n    **kwargs\n) -> Qwen3VLModel:\n    """\n    Get a Qwen3-VL model instance.\n\n    Args:\n        config: Configuration to use for the model\n        pretrained_model_name_or_path: Path to pretrained model (optional)\n        **kwargs: Additional arguments\n\n    Returns:\n        Qwen3VLModel instance\n    """\n    if pretrained_model_name_or_path:\n        return create_model_from_pretrained(pretrained_model_name_or_path, config, **kwargs)\n    else:\n        if config is None:\n            config = Qwen3VLConfig()\n        return create_model_from_config(config)