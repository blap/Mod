"""\nCore plugin system components for the optimization framework.\n\nThis module defines the fundamental interfaces and base classes for plugins,\nincluding base plugin classes, plugin interfaces, and plugin metadata.\n"""\n\nimport abc\nimport threading\nfrom typing import Any, Dict, List, Optional, Protocol, TypeVar, Generic\nfrom dataclasses import dataclass\nfrom enum import Enum\nimport logging\nimport time\nfrom contextlib import contextmanager\n\n# Import from the unified plugin base to ensure consistency\nfrom plugin_system.plugin_base import PluginState, PluginMetadata, PluginConfig, IPlugin, IOptimizationPlugin, IMemoryManagementPlugin, IAttentionPlugin, ICPUPlugin, IPowerManagementPlugin, IThermalManagementPlugin, ICacheManagementPlugin, IModelOptimizationPlugin, BasePlugin\n    PluginState,\n    PluginMetadata,\n    PluginConfig,\n    IPlugin,\n    IOptimizationPlugin,\n    IMemoryManagementPlugin,\n    IAttentionPlugin,\n    ICPUPlugin,\n    IPowerManagementPlugin,\n    IThermalManagementPlugin,\n    ICacheManagementPlugin,\n    IModelOptimizationPlugin,\n    BasePlugin\n)\n\n\nclass PluginType(Enum):\n    """Enumeration of different plugin types in the system."""\n    OPTIMIZATION = "optimization"\n    MEMORY_MANAGEMENT = "memory_management"\n    ATTENTION = "attention"\n    CPU_OPTIMIZATION = "cpu_optimization"\n    POWER_MANAGEMENT = "power_management"\n    THERMAL_MANAGEMENT = "thermal_management"\n    CACHE_MANAGEMENT = "cache_management"\n    MODEL_OPTIMIZATION = "model_optimization"\n    CUSTOM = "custom"\n\n\n# Type alias for plugin instances\nPluginTypeVar = TypeVar('PluginTypeVar', bound=IPlugin)