"""\nGradient checkpointing implementation for memory efficiency in Qwen3-VL model.\nImplements activation recomputation to trade compute for memory savings.\n"""\nimport torch\nimport torch.nn as nn\nfrom torch.utils.checkpoint import checkpoint\nfrom typing import Optional, List, Tuple\nfrom config import Qwen3VLConfig\n\n\nclass GradientCheckpointingWrapper(nn.Module):\n    """\n    Wrapper for transformer layers that enables gradient checkpointing\n    to reduce memory usage during training.\n    """\n    def __init__(self, layer: nn.Module):\n        super().__init__()\n        self.layer = layer\n\n    def forward(self, *args, **kwargs):\n        """\n        Forward pass with gradient checkpointing enabled.\n        """\n        # Use PyTorch's checkpoint function to recompute activations during backward pass\n        def create_custom_forward(module):\n            def custom_forward(*inputs, **kwdargs):\n                return module(*inputs, **kwdargs)\n            return custom_forward\n\n        # Only apply checkpointing during training\n        if self.training:\n            return checkpoint(\n                create_custom_forward(self.layer),\n                *args,\n                **kwargs,\n                use_reentrant=False  # Use non-reentrant checkpointing to avoid issues with some operations\n            )\n        else:\n            return self.layer(*args, **kwargs)\n\n\nclass Qwen3VLGradientCheckpointingConfig:\n    """\n    Configuration for gradient checkpointing in Qwen3-VL model.\n    """\n    def __init__(self, config: Qwen3VLConfig):\n        self.config = config\n        # Whether to use gradient checkpointing\n        self.use_gradient_checkpointing = config.use_gradient_checkpointing\n        # Which layers to apply gradient checkpointing to\n        self.checkpoint_layers = "all"  # Options: "all", "decoder", "vision", "none"\n        # Whether to use selective checkpointing for memory optimization\n        self.selective_checkpointing = True\n        # The interval of layers to apply checkpointing to (if using selective)\n        self.checkpoint_interval = 2  # Checkpoint every N layers\n\n\ndef apply_gradient_checkpointing(model: nn.Module, config: Qwen3VLGradientCheckpointingConfig):\n    """\n    Apply gradient checkpointing to the specified model based on the configuration.\n    """\n    if not config.use_gradient_checkpointing:\n        return model\n\n    # Apply checkpointing to decoder layers if specified\n    if config.checkpoint_layers in ["all", "decoder"]:\n        if hasattr(model, 'language_model') and hasattr(model.language_model, 'layers'):\n            for i, layer in enumerate(model.language_model.layers):\n                if config.selective_checkpointing:\n                    # Apply checkpointing every N layers\n                    if i % config.checkpoint_interval == 0:\n                        model.language_model.layers[i] = GradientCheckpointingWrapper(layer)\n                else:\n                    # Apply checkpointing to all layers\n                    model.language_model.layers[i] = GradientCheckpointingWrapper(layer)\n\n    # Apply checkpointing to vision layers if specified\n    if config.checkpoint_layers in ["all", "vision"]:\n        if hasattr(model, 'vision_tower') and hasattr(model.vision_tower, 'layers'):\n            for i, layer in enumerate(model.vision_tower.layers):\n                if config.selective_checkpointing:\n                    # Apply checkpointing every N layers\n                    if i % config.checkpoint_interval == 0:\n                        model.vision_tower.layers[i] = GradientCheckpointingWrapper(layer)\n                else:\n                    # Apply checkpointing to all layers\n                    model.vision_tower.layers[i] = GradientCheckpointingWrapper(layer)\n\n    return model\n\n\nclass MemoryEfficientAttention(nn.Module):\n    """\n    Memory-efficient attention implementation that uses gradient checkpointing\n    for the attention computation when needed.\n    """\n    def __init__(self, config: Qwen3VLConfig, layer_idx: Optional[int] = None):\n        super().__init__()\n        self.config = config\n        self.layer_idx = layer_idx\n\n        self.hidden_size = config.hidden_size\n        self.num_heads = config.num_attention_heads  # Maintains all 32 heads\n        self.head_dim = self.hidden_size // self.num_heads\n        self.num_key_value_heads = config.num_key_value_heads or self.num_heads\n        self.num_key_value_groups = self.num_heads // self.num_key_value_heads\n        self.max_position_embeddings = config.max_position_embeddings\n        self.rope_theta = config.rope_theta\n\n        if (self.head_dim * self.num_heads) != self.hidden_size:\n            raise ValueError(\n                f"hidden_size must be divisible by num_heads (got `hidden_size`: {self.hidden_size}"\n                f" and `num_heads`: {self.num_heads})."\n            )\n\n        self.q_proj = nn.Linear(self.hidden_size, self.num_heads * self.head_dim, bias=True)\n        self.k_proj = nn.Linear(self.hidden_size, self.num_key_value_heads * self.head_dim, bias=True)\n        self.v_proj = nn.Linear(self.hidden_size, self.num_key_value_heads * self.head_dim, bias=True)\n        self.o_proj = nn.Linear(self.num_heads * self.head_dim, self.hidden_size, bias=True)\n\n        self.rotary_emb = Qwen3VLRotaryEmbedding(\n            self.head_dim,\n            max_position_embeddings=self.max_position_embeddings,\n            base=self.rope_theta,\n        )\n\n        # Determine if this layer should use gradient checkpointing\n        self.use_checkpointing = config.use_gradient_checkpointing\n\n    def _compute_attention(self, query_states, key_states, value_states, attention_mask, cos, sin):\n        """\n        Internal function to compute attention, can be checkpointed.\n        All parameters must be tensors for gradient checkpointing to work properly.\n        """\n        import math\n\n        # Apply rotary embeddings\n        query_states, key_states = apply_rotary_pos_emb(query_states, key_states, cos, sin)\n\n        # Compute attention\n        attn_weights = torch.matmul(query_states, key_states.transpose(2, 3)) / math.sqrt(self.head_dim)\n\n        if attention_mask is not None:\n            causal_mask = attention_mask[:, :, :, : key_states.shape[-2]]\n            attn_weights = attn_weights + causal_mask\n\n        # Upcast attention to fp32\n        attn_weights = nn.functional.softmax(attn_weights, dim=-1, dtype=torch.float32).to(query_states.dtype)\n        attn_output = torch.matmul(attn_weights, value_states)\n        return attn_output\n\n    def forward(\n        self,\n        hidden_states: torch.Tensor,\n        attention_mask: Optional[torch.Tensor] = None,\n        position_ids: Optional[torch.LongTensor] = None,\n        past_key_value: Optional[Tuple[torch.Tensor]] = None,\n        output_attentions: bool = False,\n        use_cache: bool = False,\n        cache_position: Optional[torch.LongTensor] = None,\n    ) -> Tuple[torch.Tensor, Optional[torch.Tensor], Optional[Tuple[torch.Tensor]]]:\n        bsz, q_len, _ = hidden_states.size()\n\n        query_states = self.q_proj(hidden_states)\n        key_states = self.k_proj(hidden_states)\n        value_states = self.v_proj(hidden_states)\n\n        query_states = query_states.view(bsz, q_len, self.num_heads, self.head_dim).transpose(1, 2)\n        key_states = key_states.view(bsz, q_len, self.num_key_value_heads, self.head_dim).transpose(1, 2)\n        value_states = value_states.view(bsz, q_len, self.num_key_value_heads, self.head_dim).transpose(1, 2)\n\n        # Generate position_ids if not provided\n        if position_ids is None:\n            position_ids = torch.arange(q_len, dtype=torch.long, device=hidden_states.device).unsqueeze(0).expand(bsz, -1)\n\n        cos, sin = self.rotary_emb(value_states, position_ids)\n\n        if past_key_value is not None:\n            key_states, value_states = past_key_value.update(key_states, value_states, self.layer_idx, cache_position)\n\n        key_states = repeat_kv(key_states, self.num_key_value_groups)\n        value_states = repeat_kv(value_states, self.num_key_value_groups)\n\n        # Apply gradient checkpointing to attention computation if enabled\n        if self.use_checkpointing and self.training:\n            attn_output = checkpoint(\n                self._compute_attention,\n                query_states,\n                key_states,\n                value_states,\n                attention_mask,\n                cos,\n                sin,\n                use_reentrant=False\n            )\n        else:\n            # Apply rotary embeddings and compute attention normally\n            query_states, key_states = apply_rotary_pos_emb(query_states, key_states, cos, sin)\n            import math\n            attn_weights = torch.matmul(query_states, key_states.transpose(2, 3)) / math.sqrt(self.head_dim)\n\n            if attention_mask is not None:\n                causal_mask = attention_mask[:, :, :, : key_states.shape[-2]]\n                attn_weights = attn_weights + causal_mask\n\n            # Upcast attention to fp32\n            attn_weights = nn.functional.softmax(attn_weights, dim=-1, dtype=torch.float32).to(query_states.dtype)\n            attn_output = torch.matmul(attn_weights, value_states)\n\n        attn_output = attn_output.transpose(1, 2).contiguous()\n        attn_output = attn_output.reshape(bsz, q_len, self.hidden_size)\n\n        attn_output = self.o_proj(attn_output)\n\n        if not output_attentions:\n            attn_weights = None\n\n        return attn_output, attn_weights, past_key_value\n\n\nclass MemoryEfficientMLP(nn.Module):\n    """\n    Memory-efficient MLP implementation that uses gradient checkpointing\n    for the feed-forward computation when needed.\n    """\n    def __init__(self, config: Qwen3VLConfig):\n        super().__init__()\n        self.config = config\n        self.hidden_size = config.hidden_size\n        self.intermediate_size = config.intermediate_size\n        self.gate_proj = nn.Linear(self.hidden_size, self.intermediate_size, bias=False)\n        self.up_proj = nn.Linear(self.hidden_size, self.intermediate_size, bias=False)\n        self.down_proj = nn.Linear(self.intermediate_size, self.hidden_size, bias=False)\n        self.act_fn = nn.SiLU()\n        \n        # Configuration for memory efficiency\n        self.use_checkpointing = config.use_gradient_checkpointing\n\n    def _compute_mlp(self, x):\n        """\n        Internal function to compute MLP, can be checkpointed.\n        """\n        gate = self.gate_proj(x)\n        up = self.up_proj(x)\n        act = self.act_fn(gate)\n        intermediate = act * up\n        output = self.down_proj(intermediate)\n        return output\n\n    def forward(self, x):\n        if self.use_checkpointing and self.training:\n            return checkpoint(self._compute_mlp, x, use_reentrant=False)\n        else:\n            return self._compute_mlp(x)\n\n\nclass Qwen3VLRotaryEmbedding(nn.Module):\n    """\n    Rotary Position Embedding for Qwen3-VL model.\n    Copied from the main modeling file to maintain consistency.\n    """\n    def __init__(self, dim, max_position_embeddings=2048, base=10000):\n        super().__init__()\n        self.dim = dim\n        self.max_position_embeddings = max_position_embeddings\n        self.base = base\n        inv_freq = 1.0 / (self.base ** (torch.arange(0, self.dim, 2).float() / self.dim))\n        self.register_buffer("inv_freq", inv_freq, persistent=False)\n\n    @torch.no_grad()\n    def forward(self, x, position_ids):\n        # x: [bs, num_attention_heads, seq_len, head_size]\n        inv_freq_expanded = self.inv_freq[None, :, None].float().expand(position_ids.shape[0], -1, 1)\n        position_ids_expanded = position_ids[:, None, :].float()\n        # Force float32 since bfloat16 loses precision on long contexts\n        device_type = x.device.type\n        device_type = device_type if isinstance(device_type, str) and device_type != "mps" else "cpu"\n        with torch.autocast(device_type=device_type, enabled=False):\n            freqs = (inv_freq_expanded.float() @ position_ids_expanded.float()).transpose(1, 2)\n            emb = torch.cat((freqs, freqs), dim=-1)\n            cos = emb.cos()\n            sin = emb.sin()\n        return cos.to(dtype=x.dtype), sin.to(dtype=x.dtype)\n\n\ndef rotate_half(x):\n    """Rotates half the hidden dims of the input."""\n    x1 = x[..., : x.shape[-1] // 2]\n    x2 = x[..., x.shape[-1] // 2 :]\n    return torch.cat((-x2, x1), dim=-1)\n\n\ndef apply_rotary_pos_emb(q, k, cos, sin, unsqueeze_dim=1):\n    """Applies Rotary Position Embedding to the query and key tensors."""\n    cos = cos.unsqueeze(unsqueeze_dim)\n    sin = sin.unsqueeze(unsqueeze_dim)\n    q_embed = (q * cos) + (rotate_half(q) * sin)\n    k_embed = (k * cos) + (rotate_half(k) * sin)\n    return q_embed, k_embed\n\n\ndef repeat_kv(hidden_states: torch.Tensor, n_rep: int) -> torch.Tensor:\n    """\n    This is the equivalent of torch.repeat_interleave(x, dim=1, repeats=n_rep).\n    The hidden states go from (batch, num_key_value_heads, seqlen, head_dim) to\n    (batch, num_attention_heads, seqlen, head_dim)\n    """\n    batch, num_key_value_heads, slen, head_dim = hidden_states.shape\n    if n_rep == 1:\n        return hidden_states\n    hidden_states = hidden_states[:, :, None, :, :].expand(batch, num_key_value_heads, n_rep, slen, head_dim)\n    return hidden_states.reshape(batch, num_key_value_heads * n_rep, slen, head_dim)