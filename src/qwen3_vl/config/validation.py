"""\nValidation utilities for Qwen3-VL configuration.\n\nThis module provides validation functions for model configurations.\n"""\nfrom .config import Qwen3VLConfig\n\n\ndef validate_config(config: Qwen3VLConfig) -> bool:\n    """\n    Validate a Qwen3VLConfig instance.\n\n    Args:\n        config: Configuration to validate\n\n    Returns:\n        True if configuration is valid, False otherwise\n    """\n    # Basic validation checks\n    if config.num_hidden_layers != 32:\n        raise ValueError(f"num_hidden_layers must be 32 to preserve full capacity, got {config.num_hidden_layers}")\n\n    if config.num_attention_heads != 32:\n        raise ValueError(f"num_attention_heads must be 32 to preserve full capacity, got {config.num_attention_heads}")\n\n    if config.vocab_size <= 0:\n        raise ValueError(f"vocab_size must be positive, got {config.vocab_size}")\n\n    if config.hidden_size <= 0:\n        raise ValueError(f"hidden_size must be positive, got {config.hidden_size}")\n\n    if config.max_position_embeddings <= 0:\n        raise ValueError(f"max_position_embeddings must be positive, got {config.max_position_embeddings}")\n\n    return True\n\n\nclass ConfigValidator:\n    """A class to validate configuration objects."""\n    \n    @staticmethod\n    def validate(config: Qwen3VLConfig) -> bool:\n        """Validate the configuration."""\n        return validate_config(config)\n\n\nclass ConfigValidatorService:\n    """A service class for configuration validation."""\n    \n    def __init__(self):\n        self.validator = ConfigValidator()\n    \n    def validate(self, config: Qwen3VLConfig) -> bool:\n        """Validate the configuration using the validator."""\n        return self.validator.validate(config)