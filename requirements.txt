# Core dependencies for Inference-PIO - Self-Contained Plugin Architecture
# Optimized for various hardware configurations with plugin-based flexibility

# Core Utilities (No Heavy ML Deps)
typing-extensions>=4.8.0
psutil>=5.9.0
tqdm>=4.66.0
rich>=13.0.0
pyyaml>=6.0.0
requests>=2.31.0

# Image Processing (Pillow only)
Pillow>=10.0.0

# Development Tools
mypy>=1.7.0
black>=23.0.0
flake8>=6.0.0
pre-commit>=3.6.0
pylint>=3.0.0
isort>=5.13.0

# Testing
pytest>=7.0.0
